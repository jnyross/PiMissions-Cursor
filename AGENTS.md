# AGENTS.md

## Cursor Cloud specific instructions

This is a newly initialized repository (`PiMissions-Cursor`) with no application code, dependencies, or services yet. The repository currently contains only a `README.md`.

**When code is added**, update this section and the VM update script to reflect the actual tech stack, dependency installation commands, and service startup instructions.

### Current state

- No package manager lockfiles or dependency manifests exist.
- No build, lint, or test commands are available.
- No services to start.
